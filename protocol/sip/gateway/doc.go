// Package gateway wires the pure SIP stack (stack + transaction + uas) into a
// ready-to-run UDP UAS with logrus logging. Pure signaling — import protocol/sipmedia
// separately when RTP/voice is needed.
package gateway
